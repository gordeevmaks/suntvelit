switch (expression) {
  case 'trail':
    // Code to be executed if expression matches 'trail'
    break;
  case 'path':
    // Code to be executed if expression matches 'path'
    break;
  default:
    // Code to be executed if expression doesn't match any case
}
