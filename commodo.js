function checkMarkTooltip(markTooltip) {
    if (markTooltip === true) {
        // Your code to execute if markTooltip is true
        console.log("Tooltip is marked.");
        // You can add more actions here, like showing a tooltip or performing other related tasks
    } else {
        // Your code to execute if markTooltip is false
        console.log("Tooltip is not marked.");
    }
}

// Example usage
let markTooltip = true;  // or false, based on your logic
checkMarkTooltip(markTooltip);
